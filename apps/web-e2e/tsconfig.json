{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "allowJs": true,
    /* Callbacks passed to page.evaluate() are compiled here but execute in the
       browser, so the specs legitimately reference DOM globals. */
    "lib": ["es2022", "dom"],
    "outDir": "out-tsc/playwright",
    "sourceMap": false,
    "types": ["node"]
  },
  "include": [
    "**/*.ts",
    "**/*.js",
    "playwright.config.mts",
    "src/**/*.spec.ts",
    "src/**/*.spec.js",
    "src/**/*.test.ts",
    "src/**/*.test.js",
    "src/**/*.d.ts"
  ],
  "exclude": [
    "out-tsc",
    "test-output",
    "eslint.config.js",
    "eslint.config.mjs",
    "eslint.config.cjs"
  ]
}
