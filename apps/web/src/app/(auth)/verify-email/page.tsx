import { AuthWorkflowForm } from '../../../components/auth/auth-workflow-form';
import { Suspense } from 'react';
export default function VerifyEmailPage() { return <Suspense><AuthWorkflowForm kind="verify-email" /></Suspense>; }
