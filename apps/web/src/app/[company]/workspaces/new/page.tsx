import Link from 'next/link';

export default async function NewWorkspacePage({ params }: { params: Promise<{ company: string }> }) {
  const { company } = await params;
  return <div className="mx-auto max-w-2xl rounded-2xl border border-slate-200 bg-white p-6"><p className="text-xs font-semibold uppercase tracking-widest text-blue-700">Workspace setup</p><h1 className="mt-2 text-2xl font-bold">Create workspace</h1><p className="mt-2 text-sm text-slate-600">The typed workspace creation contract is ready. This demo keeps creation deterministic until the remaining module/NOB assignment decision is approved.</p><div className="mt-6 grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold">Workspace name<input className="mt-2 min-h-11 w-full rounded-xl border border-slate-300 px-3 font-normal" placeholder="e.g. Poultry Operations" /></label><label className="text-sm font-semibold">Workspace type<select className="mt-2 min-h-11 w-full rounded-xl border border-slate-300 px-3 font-normal"><option>Poultry</option><option>Agriculture</option><option>Piggery</option><option>Dairy</option><option>Aquaculture</option><option>Feed & Processing</option></select></label></div><div className="mt-6 flex gap-3"><button className="min-h-11 rounded-xl bg-[#0b1248] px-4 text-sm font-semibold text-white">Create draft workspace</button><Link href={`/${company}/workspaces`} className="inline-flex min-h-11 items-center rounded-xl border border-slate-300 px-4 text-sm font-semibold">Cancel</Link></div></div>;
}
